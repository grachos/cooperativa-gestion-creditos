# Solo necesario si el frontend y el backend NO comparten el mismo dominio
# (p. ej. frontend en Vercel, backend en otro proveedor). Debe incluir /api/v1.
# VITE_API_BASE_URL=https://api.midominio.com/api/v1
